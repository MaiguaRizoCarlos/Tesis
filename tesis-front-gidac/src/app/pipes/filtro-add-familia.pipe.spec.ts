import { FiltroAddFamiliaPipe } from './filtro-add-familia.pipe';

describe('FiltroAddFamiliaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAddFamiliaPipe();
    expect(pipe).toBeTruthy();
  });
});
