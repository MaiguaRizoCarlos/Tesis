import { FiltroAlturaPipe } from './filtro-altura.pipe';

describe('FiltroAlturaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAlturaPipe();
    expect(pipe).toBeTruthy();
  });
});
