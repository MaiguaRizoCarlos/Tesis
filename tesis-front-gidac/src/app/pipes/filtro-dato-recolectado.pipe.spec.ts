import { FiltroDatoRecolectadoPipe } from './filtro-dato-recolectado.pipe';

describe('FiltroDatoRecolectadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDatoRecolectadoPipe();
    expect(pipe).toBeTruthy();
  });
});
