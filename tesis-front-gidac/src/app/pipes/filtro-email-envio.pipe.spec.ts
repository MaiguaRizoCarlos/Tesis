import { FiltroEmailEnvioPipe } from './filtro-email-envio.pipe';

describe('FiltroEmailEnvioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEmailEnvioPipe();
    expect(pipe).toBeTruthy();
  });
});
