import { FiltroEquivalenciaVariableSistemaCatalogPipe } from './filtro-equivalencia-variable-sistema-catalog.pipe';

describe('FiltroEquivalenciaVariableSistemaCatalogPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEquivalenciaVariableSistemaCatalogPipe();
    expect(pipe).toBeTruthy();
  });
});
