import { FiltroEquivalenciaPipe } from './filtro-equivalencia.pipe';

describe('FiltroEquivalenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEquivalenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
