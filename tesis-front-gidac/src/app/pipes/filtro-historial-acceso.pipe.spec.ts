import { FiltroHistorialAccesoPipe } from './filtro-historial-acceso.pipe';

describe('FiltroHistorialAccesoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroHistorialAccesoPipe();
    expect(pipe).toBeTruthy();
  });
});
