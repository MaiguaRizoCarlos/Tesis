import { FiltroHistorialCambiosPipe } from './filtro-historial-cambios.pipe';

describe('FiltroHistorialCambiosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroHistorialCambiosPipe();
    expect(pipe).toBeTruthy();
  });
});
