import { FiltroPaisPipe } from './filtro-pais.pipe';

describe('FiltroPaisPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPaisPipe();
    expect(pipe).toBeTruthy();
  });
});
