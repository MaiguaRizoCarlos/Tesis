import { FiltroParcelaPipe } from './filtro-parcela.pipe';

describe('FiltroParcelaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroParcelaPipe();
    expect(pipe).toBeTruthy();
  });
});
