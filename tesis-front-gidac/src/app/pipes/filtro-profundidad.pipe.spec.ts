import { FiltroProfundidadPipe } from './filtro-profundidad.pipe';

describe('FiltroProfundidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProfundidadPipe();
    expect(pipe).toBeTruthy();
  });
});
