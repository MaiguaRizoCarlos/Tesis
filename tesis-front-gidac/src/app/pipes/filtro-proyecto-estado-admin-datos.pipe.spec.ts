import { FiltroProyectoEstadoAdminDatosPipe } from './filtro-proyecto-estado-admin-datos.pipe';

describe('FiltroProyectoEstadoAdminDatosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProyectoEstadoAdminDatosPipe();
    expect(pipe).toBeTruthy();
  });
});
