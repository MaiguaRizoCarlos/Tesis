import { FiltroProyectoEstadoPipe } from './filtro-proyecto-estado.pipe';

describe('FiltroProyectoEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProyectoEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
