import { FiltroPuntoPipe } from './filtro-punto.pipe';

describe('FiltroPuntoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPuntoPipe();
    expect(pipe).toBeTruthy();
  });
});
