import { FiltroSectorImpactoPipe } from './filtro-sector-impacto.pipe';

describe('FiltroSectorImpactoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSectorImpactoPipe();
    expect(pipe).toBeTruthy();
  });
});
