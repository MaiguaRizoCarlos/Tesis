import { FiltroSolicitudActualizarPipe } from './filtro-solicitud-actualizar.pipe';

describe('FiltroSolicitudActualizarPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSolicitudActualizarPipe();
    expect(pipe).toBeTruthy();
  });
});
