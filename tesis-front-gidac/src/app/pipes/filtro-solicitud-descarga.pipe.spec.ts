import { FiltroSolicitudDescargaPipe } from './filtro-solicitud-descarga.pipe';

describe('FiltroSolicitudDescargaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSolicitudDescargaPipe();
    expect(pipe).toBeTruthy();
  });
});
