import { FiltroTiempoEdicionDatoPipe } from './filtro-tiempo-edicion-dato.pipe';

describe('FiltroTiempoEdicionDatoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTiempoEdicionDatoPipe();
    expect(pipe).toBeTruthy();
  });
});
