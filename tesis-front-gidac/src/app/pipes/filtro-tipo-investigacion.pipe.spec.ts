import { FiltroTipoInvestigacionPipe } from './filtro-tipo-investigacion.pipe';

describe('FiltroTipoInvestigacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTipoInvestigacionPipe();
    expect(pipe).toBeTruthy();
  });
});
