import { FiltroTipoProyectoPipe } from './filtro-tipo-proyecto.pipe';

describe('FiltroTipoProyectoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTipoProyectoPipe();
    expect(pipe).toBeTruthy();
  });
});
