import { FiltroUbicacionPipe } from './filtro-ubicacion.pipe';

describe('FiltroUbicacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroUbicacionPipe();
    expect(pipe).toBeTruthy();
  });
});
