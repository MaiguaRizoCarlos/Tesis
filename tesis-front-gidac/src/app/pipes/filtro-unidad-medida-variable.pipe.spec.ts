import { FiltroUnidadMedidaVariablePipe } from './filtro-unidad-medida-variable.pipe';

describe('FiltroUnidadMedidaVariablePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroUnidadMedidaVariablePipe();
    expect(pipe).toBeTruthy();
  });
});
