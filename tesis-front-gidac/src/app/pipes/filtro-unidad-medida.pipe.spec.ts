import { FiltroUnidadMedidaPipe } from './filtro-unidad-medida.pipe';

describe('FiltroUnidadMedidaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroUnidadMedidaPipe();
    expect(pipe).toBeTruthy();
  });
});
