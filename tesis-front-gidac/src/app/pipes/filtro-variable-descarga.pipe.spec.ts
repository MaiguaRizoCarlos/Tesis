import { FiltroVariableDescargaPipe } from './filtro-variable-descarga.pipe';

describe('FiltroVariableDescargaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroVariableDescargaPipe();
    expect(pipe).toBeTruthy();
  });
});
