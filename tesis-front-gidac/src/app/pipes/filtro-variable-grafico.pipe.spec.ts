import { FiltroVariableGraficoPipe } from './filtro-variable-grafico.pipe';

describe('FiltroVariableGraficoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroVariableGraficoPipe();
    expect(pipe).toBeTruthy();
  });
});
