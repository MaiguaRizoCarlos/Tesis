import { FiltroVariablesDifusionPipe } from './filtro-variables-difusion.pipe';

describe('FiltroVariablesDifusionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroVariablesDifusionPipe();
    expect(pipe).toBeTruthy();
  });
});
