import { FiltroVaribleSistemaPipe } from './filtro-varible-sistema.pipe';

describe('FiltroVaribleSistemaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroVaribleSistemaPipe();
    expect(pipe).toBeTruthy();
  });
});
